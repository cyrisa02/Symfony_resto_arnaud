{% extends 'base.html.twig' %}

{% block title %}Modifier l'horaire
{% endblock %}

{% block body %}
	<div class="mt-4 container">
		<h1>Modifier l'horaire</h1>

		{{ include('pages/schedule/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a class="btn btn-secondary-lg mt-4" href="{{ path('app_schedule_index') }}">Retour à la liste</a>


		{{ include('pages/schedule/_delete_form.html.twig') }}</div>

{% endblock %}
