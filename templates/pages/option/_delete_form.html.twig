<form method="post" action="{{ path('app_option_delete', {'id': option.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette formule?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option.id) }}">
	<button class="btn btn-danger btn-lg mt-4">Supprimer</button>

</form>
