{% extends 'base.html.twig' %}

{% block title %}Modifier une formule
{% endblock %}

{% block body %}
	<div class="mt-4 container">
		<h1>Modifier une formule</h1>

		{{ include('pages/option/_form.html.twig', {'button_label': 'Update'}) }}

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_option_index') }}">Retour à la liste</a>


		{{ include('pages/option/_delete_form.html.twig') }}</div>

{% endblock %}
