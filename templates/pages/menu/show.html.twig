{% extends 'base.html.twig' %}

{% block title %}Menu
{% endblock %}

{% block body %}
	<div class="mt-4 container">
		<h1>Menu</h1>

		<table class="table">
			<tbody>

				<tr>
					<th>Titre</th>
					<td>{{ menu.title }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_menu_index') }}">Retour à la liste</a>


		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_menu_edit', {'id': menu.id}) }}">Modifier</a>


		{{ include('pages/menu/_delete_form.html.twig') }}</div>

{% endblock %}
