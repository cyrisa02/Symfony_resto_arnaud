{% extends 'base.html.twig' %}

{% block title %}Modifier le  Menu
{% endblock %}

{% block body %}
	<div class="mt-4 container">
		<h1>Modifier le  Menu</h1>

		{{ include('pages/menu/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_menu_index') }}">Retour à la liste</a>


		{{ include('pages/menu/_delete_form.html.twig') }}</div>

{% endblock %}
