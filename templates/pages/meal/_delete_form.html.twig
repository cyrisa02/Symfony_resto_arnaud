<form method="post" action="{{ path('app_meal_delete', {'id': meal.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce plat?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meal.id) }}">
	<button class="btn btn-danger btn-lg mt-4">Effacer</button>

</form>
