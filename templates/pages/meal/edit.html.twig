{% extends 'base.html.twig' %}

{% block title %}Modifier un plat
{% endblock %}

{% block body %}
	<div class="mt-4 container">
		<h1>Modifier un plat</h1>

		{{ include('pages/meal/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a class="btn btn-secondary btn-lg mt-4" href="{{ path('app_meal_index') }}">Retour à la liste</a>


		{{ include('pages/meal/_delete_form.html.twig') }}</div>

{% endblock %}
